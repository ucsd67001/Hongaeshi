# このリポジトリで作業するときに、最初に読むもの

⚠️ **新しいスレッドは、前のスレッドの会話を見ていない。**
引き継がれるのはここにあるファイルだけ。決めたことは会話に残さず、必ずここへ書く。

## まず README.md を読む

作りの要点・決めたこと・保留していることは、すべて `README.md` にある。
とくに次の7つは、知らずに触ると壊す。

1. **Firestore の中だけ、名前が ASCII。**ルールの言語が日本語の識別子を受け付けない。
   翻訳しているのは `public/共通.js` だけ。
2. **表示名・アイコンを記録に焼き付けない。**`users/{uid}` に持ち、画面で引く。
3. **お金の整合性は `firestore.rules` の `getAfter()` だけで守っている。**
   財布の更新と返しの作成は、必ず1つの `writeBatch` で送る。
4. **onclick に値を渡すときは `引数()`、文字として出すときは `逃()`。**
   `逃()` だけで onclick に入れると、URL から任意のスクリプトを動かせる（実際に穴だった）。
5. **お金の計算は `共通.js` の `受取人へ` / `額の内訳` / `内訳を作る` だけで。**`0.9` を直に書かない。
6. **窓（覆い）は `窓を出す()` で開き、`覆い閉じ()` で閉じる。**HTML を自分で組まない。
7. **著者・出版社の主体を作るときは `04_tools/書誌.mjs` の `無い主体を作る()` を通す。**
   `merge:true` で書いても既存の `claimed` や別名は上書きされる（実際にそうなっていた）。

## 動かすときの環境変数

```bash
export HONGAESHI_KEY="/c/Users/ucsd6/.hongaeshi/鍵.json"
export GOOGLE_BOOKS_KEY="$(cat '/c/Users/ucsd6/.hongaeshi/books api.txt' | tr -d '\r\n ')"
```

## デプロイ

```bash
firebase deploy --project hongaeshi --only hosting
```

⚠️ **`--project hongaeshi` を必ず付ける。**環境に別のプロジェクトIDが入っていて、
付けないと `Failed to get Firebase project studio-…` で落ちる。

ルールを変えたときは `--only firestore:rules` / `--only storage` も。

Functions を出すとき：

```bash
FUNCTIONS_DISCOVERY_TIMEOUT=60000 firebase deploy --project hongaeshi --only functions --non-interactive
```

⚠️ **待ち時間を延ばす。**既定の10秒だと `User code failed to load … Timeout after 10000` で落ちる（2026-09-26）。
⚠️ **Functions と hosting を一緒に出さない。**Functions は出ても、最後に「cleanup policy」のエラーが出ると
hosting が公開されないまま終わる（2026-09-26 に踏んだ）。hosting は別に出して、画面で確かめる。
（そのエラーの元の「古い処理の中身を消す設定」は、2026-09-26 に入れた。1日より古いものを自動で消す）

## 本を増やすとき

README の「本を増やす手順」の順に流す。**「著者を直す」を飛ばさない。**

利用者からの申請を処理するときは、README の「申請を処理する（案B）」の順。
**最後に `node 04_tools/申請.mjs 並べた <申請id> <ISBN>` を忘れない**（申請した人への記録が抜ける）。

⚠️ **棚にある作品の別の版（文庫・単行本など）は、新しい本にしない。**
`04_tools/版を足す.mjs` で作品のページに足す（1作品1ページ。README「単行本と文庫は、1作品1ページ」）。
`版を差し替える.mjs` は記録が消えるので使わない。

## 道具を使うときの落とし穴（実際に踏んだもの）

⚠️ **Bash は日本語の変数名を扱えない。**
`題="三剣物語"` は `command not found` になり、`$題` はそのまま文字列として渡る。
**題名が `$題` のまま登録された**（2026-09-23）。引数は直接書くこと。

⚠️ **Python の `str.replace` で一括置換しない。**
エスケープを含む文字列で黙って失敗する。Edit ツールを使い、毎回確かめる。

⚠️ **`curl` で Amazon の画像を判定しない。**相手を見て返すものが変わる。
判定はブラウザの中で `naturalWidth` を見る（`public/app.js` の `表紙をみる`）。

## 作業の進め方（この人の好み）

- **まず壁打ち。**作る前に、選択肢と理由を出して相談する
- 判断が要ることは、勝手に決めずに聞く。**理由を添えて選択肢を出す**と早い
- 変えたら**デプロイして、実際の画面で確かめてから**報告する
- 直したら **README に記録して push** まで
- うまくいかなかったことは、そのまま言う。取り繕わない
